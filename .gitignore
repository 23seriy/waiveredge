# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
.env.local
backend/.env

# Node / Next
node_modules/
.next/
out/
next-env.d.ts
npm-debug.log*
*.tsbuildinfo

# Tooling
.serena/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local SSL certs (for Yahoo OAuth dev)
*.pem

# Data: the real fixtures are reproducible via scripts/dump_real_fixtures.py
# (and re-materialized by the app on demand), so keep the JSON out of git.
backend/sample_data/*.json
backend/sample_data_mlb/*.json
